class Products {
    constructor() {
        this.products = []
    }

    add(product) {
        const prodExists = this.products.find((product) => product.id === product)
        if (prodExists !== undefined) {
            
        } else {

        }
    }
}